# Brainstorming/scratch — not part of the project
.superpowers/
.tmp/

# Claude Code per-machine permission overrides (not shared)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Env
.env
.env.local
.env.*.local

# Node / Bun (will apply once Phase 0 lands)
node_modules/
.next/
out/
*.log
.turbo/
coverage/
# Next.js auto-generated TypeScript reference — content differs between
# `next dev` (`./.next/dev/types/...`) and `next build` (`./.next/types/...`).
# Regenerated on every dev/build invocation; never commit.
next-env.d.ts
playwright-report/
test-results/
.lighthouseci/


# Contentlayer
.contentlayer

# TypeScript incremental build cache
*.tsbuildinfo

# Raw content inputs (large photos, screenshots, audio) — not for git
content-input/*
!content-input/README.md
