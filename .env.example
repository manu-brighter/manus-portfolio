# Public-facing env vars (NEXT_PUBLIC_*) — kept here as documentation and template.
# Copy to .env.local for development. .env.local is gitignored.

# When true, robots.txt issues a global disallow. Use for preview/staging deploys.
NEXT_PUBLIC_ROBOTS_DISALLOW=false
